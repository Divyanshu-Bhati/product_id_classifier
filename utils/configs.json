{
    "inputs_path": "data/",
    "training_history": "training_history",
    "random_seed": 42,
    "data_filters": {
      "max_len": 14,
      "min_len": 10,
      "remove_non_ascii": true
    },
    "vae_configs": {
        "continue_training": false,
        "early_stopping": true,
        "epochs": 100,
        "num_workers": 0,
        "hyperparameters": 
        {
            "batch_size": 2048,
            "embedding_dim": 64,
            "latent_dim": 128,
            "hidden_dim": 128,
            "learning_rate": 0.001,
            "patience": 10
        }
    },
    "cls_configs": {
        "epochs": 100,
        "num_workers": 0,
        "hyperparameters":
        {
            "batch_size": 2048,
            "learning_rate": 0.001,
            "patience": 10
        }
    }
}